{% render 'breadcrumbs' %}
<div class="page-compare mb-40">
    {%- if customer -%}
        {% render 'compare' %}
    {%- else -%}
        <script>
            window.location.href = '/account/login';
        </script>
    {%- endif -%}
</div>