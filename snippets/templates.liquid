<div class="theme-templates">
    {% comment %}
    <template id="template-popup-search-ajax">
        <div class="col-12 col-lg-2">
            {% render 'product-search', product: null %}
        </div>
    </template>
    <template id="template-search-ajax">
        <div class="col-12 mb-10">
            {% render 'product-search-2', product: null %}
        </div>
    </template>
    {% endcomment %}
    {%- assign namespaces = 'wishlist,compare' | split: ',' -%}
    {%- for namespace in namespaces -%}
        <template id="template-{{ namespace }}-ajax">
            <div>
                {% render 'product-store-lists' %}
            </div>
        </template>
    {%- endfor -%}
    <template id="template-compare-full-ajax">
        <div>
            {% render 'product-compare' %}
        </div>
    </template>
    <template id="template-wishlist-full-ajax">
        <div>
            {% render 'product-wishlist' %}
        </div>
    </template>
</div>